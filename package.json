{
  "name": "mercysoul-os",
  "version": "2.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node agent-bootstrap.js",
    "test": "node --test src/*.test.js",
    "check": "node --check server.js && node --check agent-bootstrap.js && node --check src/sealed-gate.js && node --check src/agent/mercysoul-graph.js && npm test"
  },
  "dependencies": {
    "@langchain/langgraph": "1.4.13",
    "@vercel/firewall": "latest",
    "express": "^4.18.2",
    "multer": "^2.0.2"
  }
}
