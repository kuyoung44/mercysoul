name: MercySoul OS — Build, Verify & Report

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '17 3 * * 1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 20
      - name: Install
        run: npm install
      - name: Code checks
        run: npm run check
      - name: Dependency audit
        run: npm audit --audit-level=high || true
      - name: Build report
        run: |
          mkdir -p reports
          {
            echo "# MercySoul OS automated report"
            echo
            echo "- Commit: $GITHUB_SHA"
            echo "- Trigger: $GITHUB_EVENT_NAME"
            echo "- Node: $(node --version)"
            echo "- npm: $(npm --version)"
            echo "- Store adapter: Supabase when configured; memory fallback otherwise"
            echo "- Vision Brain: enabled"
            echo "- Status: code verification completed"
          } > reports/build-report.md
      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: mercysoul-build-report
          path: reports/build-report.md
