services:
  - type: web
    name: mercysoul-os
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node agent-bootstrap.js
    healthCheckPath: /health
    autoDeploy: true
