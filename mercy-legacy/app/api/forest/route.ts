import{NextResponse}from"next/server";import{getAdminDb}from"@/lib/supabase/admin";export async function GET(){try{const db=getAdminDb();const{data,error}=await db.from("legacy_trees").select("id,tree_code,species,planted_at,latitude,longitude,site_name,status,legacy_profiles!inner(legacy_code,full_name,visibility)").in("status",["verified","living"]).eq("legacy_profiles.visibility","public");if(error)throw error;return NextResponse.json({trees:data||[]})}catch(e:any){return NextResponse.json({error:e.message||"Forest unavailable."},{status:500})}}