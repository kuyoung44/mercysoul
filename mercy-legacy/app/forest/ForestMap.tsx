"use client";import {MapContainer,TileLayer,CircleMarker,Popup} from "react-leaflet";import "leaflet/dist/leaflet.css";import Link from "next/link";type Tree={id:string;tree_code:string;species:string|null;latitude:number|null;longitude:number|null;site_name:string|null;status:string;legacy_profiles:any};export default function ForestMap({trees}:{trees:Tree[]}){const located=trees.filter(t=>typeof t.latitude==="number"&&typeof t.longitude==="number");const center:[number,number]=located.length?[located[0].latitude!,located[0].longitude!]:[9.082,8.6753];return <div className="mapShell"><MapContainer center={center} zoom={located.length?5:6} scrollWheelZoom className="leafletMap"><TileLayer attribution="&copy; OpenStreetMap contributors" url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/>{located.map(t=><CircleMarker key={t.id} center={[t.latitude!,t.longitude!]} radius={9}><Popup><strong>{t.tree_code}</strong><br/>{t.species||"Tree"}<br/>{t.site_name||"Legacy Forest"}<br/><Link href={"/legacy/"+t.legacy_profiles.legacy_code}>Open legacy</Link></Popup></CircleMarker>)}</MapContainer><div className="mapMeta">{trees.length} verified/living tree{trees.length===1?"":"s"} · {located.length} mapped</div></div>}