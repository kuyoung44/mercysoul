"use client";
import {useState} from "react"; import Link from "next/link";
export default function Create(){const [name,setName]=useState("");const [story,setStory]=useState("");const [done,setDone]=useState(false);async function submit(e:any){e.preventDefault();const r=await fetch("/api/legacy",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({full_name:name,story})});if(r.ok)setDone(true)}return <main className="wrap"><nav className="nav"><Link className="brand" href="/">MERCY<span className="gold">LEGACY</span></Link></nav><section className="form"><div className="eyebrow">CREATE</div><h1>Create a Legacy</h1>{done?<div className="card"><h2>Legacy created.</h2><p>Your living identity is ready.</p><Link className="btn" href="/forest">Open Forest Map</Link></div>:<form onSubmit={submit}><label>Full name</label><input className="input" required value={name} onChange={e=>setName(e.target.value)}/><label>Life story</label><textarea className="input" rows={8} value={story} onChange={e=>setStory(e.target.value)} placeholder="Tell the story you want remembered..."/><button className="btn">Create Legacy</button></form>}</section></main>}