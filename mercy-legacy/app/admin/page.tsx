import Link from"next/link";import{redirect}from"next/navigation";import{requireAdmin}from"@/lib/auth";import{getAdminDb}from"@/lib/supabase/admin";export default async function Admin(){const user=await requireAdmin();if(!user)redirect("/auth/login?next=/admin");const db=getAdminDb();const[{count:profiles},{count:trees},{count:sites},{data:audit}]=await Promise.all([db.from("legacy_profiles").select("*",{count:"exact",head:true}),db.from("legacy_trees").select("*",{count:"exact",head:true}),db.from("forest_sites").select("*",{count:"exact",head:true}),db.from("legacy_audit").select("*").order("created_at",{ascending:false}).limit(8)]);return <main className="wrap"><nav className="nav"><Link className="brand" href="/">MERCY<span className="gold">LEGACY</span></Link><div className="links"><Link href="/plant">Plant</Link><form action="/auth/signout" method="post"><button className="linkButton">Sign out</button></form></div></nav><section className="hero"><div className="eyebrow">CONTROL CENTER · {user.email}</div><h1>Admin Dashboard</h1><div className="grid"><div className="card stat"><strong>{profiles||0}</strong><span>Legacy Profiles</span></div><div className="card stat"><strong>{trees||0}</strong><span>Trees</span></div><div className="card stat"><strong>{sites||0}</strong><span>Forest Sites</span></div><div className="card stat"><strong>{audit?.length||0}</strong><span>Recent Audit</span></div></div><div className="card"><h2>Operations</h2><p>Authenticated workspace for legacy identities, tree registration, verification and publication.</p><div className="actions"><Link className="btn" href="/create">Create Legacy</Link><Link className="btn alt" href="/plant">Register Tree</Link><Link className="btn alt" href="/forest">Forest Map</Link></div></div></section></main>}