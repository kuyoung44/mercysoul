import"./globals.css";export const metadata={title:"MercyLegacy — Create · Plant · Connect",description:"Where memory becomes living legacy — identities, living trees, QR connections and forests."};export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}