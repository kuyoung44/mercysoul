import Link from "next/link"; import QR from "qrcode.react"; export default async function QRPage({params}:{params:Promise<{code:string}>}){const {code}=await params;const origin=process.env.NEXT_PUBLIC_SITE_URL||"https://mercy-legacy.vercel.app";const url=origin+"/legacy/"+code;return <main className="wrap"><section className="hero" style={{textAlign:"center"}}><div className="eyebrow">QR IDENTITY</div><h1>{code}</h1><div className="card" style={{display:"inline-block",background:"#fff"}}><QR value={url} size={220}/></div><p className="lead" style={{margin:"25px auto"}}>Scan this identity to open the connected legacy profile.</p><Link className="btn" href={"/legacy/"+code}>Open Profile</Link></section></main>}