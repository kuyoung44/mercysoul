import{getServerSupabase}from"./supabase/server";export async function getUser(){const supabase=await getServerSupabase();const{data:{user}}=await supabase.auth.getUser();return user}export async function requireAdmin(){const user=await getUser();if(!user)return null;const allow=(process.env.ADMIN_EMAILS||"").split(",").map(x=>x.trim().toLowerCase()).filter(Boolean);return allow.includes((user.email||"").toLowerCase())||user.app_metadata?.role==="admin"?user:null}